# JAX GPU memory settings.
# Disable upfront preallocation so JAX shares the GPU with other processes.
# Without this, JAX tries to reserve ~90% of all GPU memory on startup.
XLA_PYTHON_CLIENT_PREALLOCATE=false
